// import { createSelector } from 'reselect';

// const categorySelector = state => state.categoryReducer;
// const selectCategory = createSelector(categorySelector)


// export default selectCategory;
